// Remove the floating navigation component entirely since we're now using the AVES logo in the header
// This component is no longer needed with the new mobile navigation design

export default function FloatingAVESNavigation() {
  // Component disabled - navigation now handled by header AVES logo
  return null
}
